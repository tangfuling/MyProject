# 公众号数据运营助手 — 技术文档

## 1. 系统架构

```
┌──────────────────────┐      ┌──────────────────────────────┐
│   Chrome Extension   │      │   Web App (SPA)              │
│   (Content Script +  │      │   gzh.niuma.com         │
│    Popup + BG)       │      │                              │
└──────────┬───────────┘      └──────────────┬───────────────┘
           │                                  │
           │  POST /sync/articles             │  REST API + SSE
           ▼                                  ▼
┌──────────────────────────────────────────────────────────┐
│                  Backend API Server                       │
│                  api-gzh.niuma.com                       │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ 认证模块  │  │ 数据存储  │  │ AI 引擎  │  │ 计费引擎│ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
│                                    │                     │
│                                    ▼                     │
│                           ┌──────────────┐               │
│                           │  Claude API  │               │
│                           └──────────────┘               │
└──────────────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────┐  ┌──────────────┐  ┌─────────────┐
│   MySQL / PostgreSQL │  │    Redis     │  │  支付宝     │
│   (主数据存储)        │  │  (验证码缓存) │  │  (支付回调)  │
└──────────────────────┘  └──────────────┘  └─────────────┘
```

---

## 2. 项目标识

- **包名**：`com.niuma.gzh`
- **Web 域名**：`gzh.niuma.com`（示例）
- **API 域名**：`api-gzh.niuma.com`（示例）

---

## 3. 技术选型

| 模块 | 技术 | 说明 |
|---|---|---|
| Chrome 插件 | Manifest V3 + Vanilla JS | 轻量，无需框架 |
| Web 前端 | React + Vite + TailwindCSS | 现代 SPA，快速开发 |
| 后端 | Node.js (Express/Koa) 或 Python (FastAPI) | 选熟悉的即可 |
| 数据库 | MySQL 或 PostgreSQL | 关系型，结构化数据 |
| 缓存 | Redis | 短信验证码、登录 session |
| AI | Claude API | 分析报告 + 对话，支持 function calling |
| 支付 | 支付宝当面付/手机网站支付 | 已有资质 |
| 部署 | 云服务器 + Nginx | 反向代理，HTTPS |

---

## 4. Chrome 插件技术方案

### 4.1 文件结构

```
extension/
├── manifest.json          # Manifest V3 配置
├── background.js          # Service Worker，管理登录态
├── content-script.js      # 注入微信后台页面，数据采集
├── popup.html             # 弹窗界面
├── popup.js               # 弹窗逻辑
└── icons/                 # 插件图标
```

### 4.2 manifest.json 关键配置

```json
{
  "manifest_version": 3,
  "name": "公众号数据运营助手",
  "permissions": ["activeTab", "storage"],
  "host_permissions": [
    "https://mp.weixin.qq.com/*",
    "https://api-gzh.niuma.com/*"
  ],
  "content_scripts": [{
    "matches": ["https://mp.weixin.qq.com/*"],
    "js": ["content-script.js"]
  }],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html"
  }
}
```

### 4.3 数据采集流程

```
content-script.js 注入 mp.weixin.qq.com
        │
        ▼
 渲染悬浮按钮（页面右下角）
        │
        ▼ 用户点击
 提取当前页面 token（URL 参数）
        │
        ▼
 Step 1: 获取已发布文章列表
   GET /cgi-bin/appmsgpublish?sub=list&begin=0&count=10&token={token}
   → 分页遍历，提取所有文章 ID、标题、发布时间
        │
        ▼
 Step 2: 获取每篇文章阅读数据
   GET /misc/appmsganalysis?action=detailpage&msgid={mid}_1&publish_date={date}&token={token}
   → 提取：阅读、分享、点赞、在看、评论、收藏、完读率、来源分布
        │
        ▼
 Step 3: 获取文章全文（新文章）
   GET /s/{article_id}（带 cookies）
   → 提取正文纯文本，去 HTML 标签
        │
        ▼
 Step 4: 发送到后端
   POST https://api-gzh.niuma.com/sync/articles
   Body: { articles: [...], snapshots: [...] }
```

### 4.4 关键实现细节

- **Cookie 自动携带**：Content Script 在微信域下运行，fetch 请求自动携带 cookie
- **Token 提取**：从当前页面 URL 的 `token` 参数中获取
- **增量同步逻辑**：
  - 通过 `chrome.storage.local` 记录上次同步的文章 ID 列表
  - 新文章：全量抓取（列表 + 数据 + 全文）
  - 已有文章：只更新数据指标（追加 snapshot）
- **进度通知**：通过 `chrome.runtime.sendMessage` 向 popup 发送进度更新

---

## 5. 后端 API 设计

### 5.1 API 列表

| 方法 | 路径 | 说明 | 鉴权 |
|---|---|---|---|
| POST | `/auth/send-code` | 发送短信验证码 | 无 |
| POST | `/auth/login` | 验证码登录/注册 | 无 |
| POST | `/sync/articles` | 接收插件同步的数据 | JWT |
| GET | `/articles` | 获取文章列表（支持时间筛选、分页） | JWT |
| GET | `/articles/overview` | 获取指定时间范围的聚合概览 | JWT |
| POST | `/analysis/generate` | 生成 AI 分析报告 | JWT |
| GET | `/analysis/reports` | 获取历史分析报告列表 | JWT |
| GET | `/analysis/reports/:id` | 获取单份报告详情 | JWT |
| POST | `/chat/send` | 发送对话消息（SSE 流式响应） | JWT |
| GET | `/chat/history` | 获取对话历史 | JWT |
| GET | `/user/profile` | 获取用户信息（含余额） | JWT |
| GET | `/user/token-logs` | 获取消费记录 | JWT |
| POST | `/payment/create` | 创建支付订单 | JWT |
| POST | `/payment/notify` | 支付宝异步回调 | 支付宝签名验证 |

### 5.2 核心接口详情

#### POST /auth/send-code

```json
// Request
{ "phone": "13800001234" }

// Response
{ "code": 0, "message": "验证码已发送" }
```

- 验证码存 Redis，TTL 5 分钟
- 频率限制：同一手机号 60 秒内不可重发

#### POST /auth/login

```json
// Request
{ "phone": "13800001234", "code": "123456" }

// Response
{
  "code": 0,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "user": {
      "id": 1,
      "phone": "138****1234",
      "balance": 100,
      "free_quota": 100
    }
  }
}
```

- 新用户自动注册，初始化 `free_quota = 100`（¥1.00）
- 返回 JWT token，有效期 30 天

#### POST /sync/articles

```json
// Request
{
  "articles": [
    {
      "wx_article_id": "abc123",
      "title": "文章标题",
      "content": "纯文本全文...",
      "word_count": 1500,
      "publish_time": "2026-03-14T10:00:00Z"
    }
  ],
  "snapshots": [
    {
      "wx_article_id": "abc123",
      "read_count": 120,
      "share_count": 8,
      "like_count": 6,
      "wow_count": 5,
      "comment_count": 3,
      "save_count": 2,
      "completion_rate": 67.5,
      "traffic_sources": {
        "公众号消息": 24,
        "朋友圈": 82,
        "好友转发": 8,
        "推荐": 3,
        "其他": 3
      },
      "new_followers": 1
    }
  ]
}

// Response
{ "code": 0, "data": { "new_articles": 5, "updated_articles": 30 } }
```

- 按 `wx_article_id` 去重，已有文章只追加 snapshot
- 新文章存储全文 + 创建首条 snapshot

#### GET /articles/overview

```
GET /articles/overview?range=30d
```

```json
// Response
{
  "code": 0,
  "data": {
    "range": "30d",
    "article_count": 8,
    "metrics": {
      "total_read": 640,
      "avg_read": 80,
      "completion_rate": 62.0,
      "total_share": 48,
      "total_like": 36,
      "new_followers": 12
    },
    "changes": {
      "total_read": 12.0,
      "avg_read": 5.0,
      "completion_rate": -3.0,
      "total_share": 20.0,
      "total_like": 8.0,
      "new_followers": 50.0
    },
    "traffic_summary": {
      "朋友圈": 52,
      "公众号消息": 30,
      "搜一搜": 8,
      "推荐": 5,
      "其他": 5
    }
  }
}
```

- `changes` 为与上一个同等时间段的环比百分比

#### POST /analysis/generate

```json
// Request
{ "range": "30d" }

// Response (SSE stream)
data: {"type": "chunk", "content": "▎你现在在什么阶段\n"}
data: {"type": "chunk", "content": "你已稳定更新 78 天..."}
...
data: {"type": "done", "report_id": 42, "tokens_in": 18000, "tokens_out": 2150, "cost": 18}
```

- 流式输出，前端实时渲染
- 结束时返回报告 ID、token 消耗、费用
- 报告持久化到 `analysis_reports` 表

#### POST /chat/send

```json
// Request
{ "message": "我的外向型文章和记录型文章，数据差异的根本原因是什么？" }

// Response (SSE stream)
data: {"type": "chunk", "content": "从数据看，差异主要来自..."}
...
data: {"type": "function_call", "name": "load_article", "args": {"article_id": 42}}
data: {"type": "chunk", "content": "具体看这篇《...》..."}
...
data: {"type": "done", "tokens_used": 2340, "cost": 2}
```

---

## 6. 数据库设计

### 6.1 ER 图

```
users 1──N articles 1──N article_snapshots
  │
  ├──N conversations
  ├──N analysis_reports
  ├──N token_logs
  └──N payments
```

### 6.2 表结构

#### users

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| phone | VARCHAR(20) UNIQUE NOT NULL | 手机号 |
| balance | INT DEFAULT 0 | 余额（单位：分） |
| free_quota | INT DEFAULT 100 | 剩余免费额度（分） |
| created_at | DATETIME DEFAULT NOW() | 注册时间 |
| updated_at | DATETIME | 更新时间 |

#### articles

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT NOT NULL FK→users.id | 用户 ID |
| wx_article_id | VARCHAR(64) NOT NULL | 微信文章唯一标识 |
| title | VARCHAR(256) | 标题 |
| content | TEXT | 纯文本全文 |
| word_count | INT | 字数 |
| publish_time | DATETIME | 发布时间 |
| created_at | DATETIME DEFAULT NOW() | 首次同步时间 |

- 唯一索引：`(user_id, wx_article_id)`

#### article_snapshots

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| article_id | BIGINT NOT NULL FK→articles.id | 关联文章 |
| read_count | INT | 阅读数 |
| share_count | INT | 分享数 |
| like_count | INT | 点赞数 |
| wow_count | INT | 喜欢（在看）数 |
| comment_count | INT | 留言数 |
| save_count | INT | 收藏数 |
| completion_rate | DECIMAL(5,2) | 完读率 |
| traffic_sources | JSON | 来源占比 |
| new_followers | INT | 文章带来的新增关注 |
| synced_at | DATETIME DEFAULT NOW() | 同步时间 |

- 索引：`(article_id, synced_at)`

#### analysis_reports

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT NOT NULL FK→users.id | 用户 ID |
| time_range | VARCHAR(16) | 分析范围（7d/30d/90d/all） |
| article_count | INT | 分析文章数 |
| content | TEXT | 报告内容（Markdown） |
| recommended_questions | JSON | 推荐问题（5 个） |
| tokens_in | INT | 输入 token |
| tokens_out | INT | 输出 token |
| cost | INT | 费用（分） |
| created_at | DATETIME DEFAULT NOW() | 生成时间 |

#### conversations

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT NOT NULL FK→users.id | 用户 ID |
| messages | JSON | 完整对话历史 |
| token_used | INT | 累计 token 消耗 |
| created_at | DATETIME DEFAULT NOW() | 创建时间 |
| updated_at | DATETIME | 最后更新 |

#### token_logs

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT NOT NULL FK→users.id | 用户 ID |
| action | VARCHAR(32) | analysis / chat |
| tokens_in | INT | 输入 token |
| tokens_out | INT | 输出 token |
| cost | INT | 费用（分） |
| detail | JSON | 补充信息 |
| created_at | DATETIME DEFAULT NOW() | 时间 |

- 索引：`(user_id, created_at)`

#### payments

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT NOT NULL FK→users.id | 用户 ID |
| amount | INT | 金额（分） |
| channel | VARCHAR(32) | alipay |
| status | VARCHAR(16) | pending / success / failed |
| trade_no | VARCHAR(64) | 第三方交易号 |
| created_at | DATETIME DEFAULT NOW() | 时间 |

---

## 7. AI 引擎设计

### 7.1 分层喂数据策略

```
第一层：全量概览（每次必传）
  所有文章：标题 + 发布时间 + 字数 + 阅读/分享/收藏/完读率/来源占比
  → 100 篇 ≈ 3,000-5,000 tokens
  → 模型看到全局趋势、对比排序

第二层：选定范围全文（按用户选择的时间范围）
  如近 30 天的 8 篇文章全文
  → 8 篇 × 1,500 字 ≈ 8,000-12,000 tokens
  → 模型分析内容质量、风格、钩子、结构

第三层：对话时按需加载（function calling）
  用户提到具体文章时，模型请求加载那篇全文
  → 单篇 ≈ 1,500-2,000 tokens
```

### 7.2 对话 Context 构成

```
每轮对话传入：
├── System Prompt（角色设定 + 分析框架）      ≈ 500 tokens
├── 全量概览（始终带着）                      ≈ 4,000 tokens
├── 当前分析报告（已生成的）                   ≈ 1,500 tokens
├── 按需加载的文章全文                         ≈ 0-2,000 tokens
└── 对话历史                                  ≈ 变量
```

### 7.3 分析报告 Prompt 框架

```
你是一个公众号数据运营分析师。以下是用户公众号的全部数据。

## 全量概览
{全量概览 JSON}

## 选定范围文章全文
{文章全文列表}

请输出以下结构的分析报告：

1. **你现在在什么阶段**：用大白话说清楚，禁止使用"冷启动期""内容验证期"等术语
2. **核心发现**：3-5 条，分正向（✓）和负向（✗），必须引用具体数据
3. **可执行建议**：3 条，每条必须是本周能做的具体动作
4. **节奏感**：基于数据事实给出理性鼓励，不鸡汤

最后，输出 5 个推荐问题，放在 recommended_questions 字段。
要求：基于这个用户的数据生成，口语化，≤15 字，让用户想点。

输出格式：Markdown 正文 + 末尾 JSON 块包含 recommended_questions。
```

### 7.4 Function Calling 定义

```json
{
  "name": "load_article_content",
  "description": "加载指定文章的全文内容，当用户提到具体文章或需要分析某篇文章细节时调用",
  "parameters": {
    "type": "object",
    "properties": {
      "article_id": {
        "type": "integer",
        "description": "文章 ID"
      }
    },
    "required": ["article_id"]
  }
}
```

---

## 8. 关键流程时序

### 8.1 数据同步

```
浏览器(微信后台)    Content Script    Background    Backend
     │                   │                │            │
     │  页面加载          │                │            │
     │──────────────────>│                │            │
     │                   │ 注入悬浮按钮    │            │
     │                   │                │            │
     │  点击同步          │                │            │
     │──────────────────>│                │            │
     │                   │ 提取 token      │            │
     │                   │                │            │
     │                   │ fetch 文章列表  │            │
     │                   │───────────────>│            │
     │                   │<───────────────│            │
     │                   │                │            │
     │                   │ fetch 每篇数据  │            │
     │                   │───────────────>│            │（循环）
     │                   │<───────────────│            │
     │                   │                │            │
     │                   │ POST /sync/articles         │
     │                   │────────────────────────────>│
     │                   │<────────────────────────────│
     │                   │                │            │
     │  显示完成 + 跳转   │                │            │
     │<──────────────────│                │            │
```

### 8.2 生成分析报告

```
Web 前端              Backend              Claude API
  │                     │                      │
  │ POST /analysis/generate                    │
  │────────────────────>│                      │
  │                     │ 查询用户文章数据       │
  │                     │ 构建分层 prompt        │
  │                     │                      │
  │                     │ 检查余额是否充足       │
  │                     │                      │
  │                     │ 调用 Claude API (stream)
  │                     │─────────────────────>│
  │  SSE: chunk         │                      │
  │<────────────────────│<─────────────────────│
  │  SSE: chunk         │                      │
  │<────────────────────│<─────────────────────│
  │  ...                │                      │
  │                     │ 保存报告到数据库       │
  │                     │ 记录 token_log        │
  │                     │ 扣减余额              │
  │  SSE: done          │                      │
  │<────────────────────│                      │
```

### 8.3 对话（含 Function Calling）

```
Web 前端              Backend              Claude API
  │                     │                      │
  │ POST /chat/send     │                      │
  │────────────────────>│                      │
  │                     │ 构建 context          │
  │                     │ (system + 概览 +      │
  │                     │  报告 + 对话历史)      │
  │                     │                      │
  │                     │ 调用 Claude (stream)   │
  │                     │─────────────────────>│
  │                     │                      │
  │                     │ function_call:        │
  │                     │ load_article_content  │
  │                     │<─────────────────────│
  │                     │                      │
  │                     │ 查询文章全文           │
  │                     │ 将全文作为结果返回      │
  │                     │─────────────────────>│
  │                     │                      │
  │  SSE: chunk         │                      │
  │<────────────────────│<─────────────────────│
  │  SSE: chunk         │                      │
  │<────────────────────│<─────────────────────│
  │  SSE: done          │                      │
  │<────────────────────│                      │
```

---

## 9. 计费引擎

### 9.1 计费流程

```
每次 AI 调用：
1. 预检查：余额 + 免费额度 > 0，否则拒绝
2. 调用完成后，从 Claude API 响应获取实际 token 用量
3. 计算费用（按 Claude API 定价换算为人民币）
4. 扣费顺序：优先扣 free_quota，不足部分扣 balance
5. 写入 token_logs 记录明细
6. 更新 users 表余额
7. 以上步骤在数据库事务中完成
```

### 9.2 费用计算

```
cost = (tokens_in × input_price + tokens_out × output_price) × exchange_rate

其中：
- input_price / output_price：Claude API 单价（美元/token）
- exchange_rate：美元兑人民币汇率（可配置）
- 结果四舍五入到分
```

---

## 10. 安全设计

| 风险 | 措施 |
|---|---|
| JWT 泄露 | Token 有效期 30 天，支持服务端吊销 |
| 短信轰炸 | 同一手机号 60 秒限频，同一 IP 每小时 10 次 |
| 数据越权 | 所有 API 通过 JWT 中的 user_id 限定数据范围 |
| 支付安全 | 支付宝异步回调使用签名验证，金额与订单匹配 |
| 余额扣费 | 数据库事务 + 乐观锁，防止并发超扣 |
| 微信接口变更 | 预留 CSV 手动上传降级方案（V2） |

---

## 11. 部署方案

```
┌─────────────────────────────────┐
│            Nginx                │
│  gzh.niuma.com → Web 静态  │
│  api-gzh.niuma.com → Backend   │
│  (HTTPS, Let's Encrypt)        │
└──────────────┬──────────────────┘
               │
    ┌──────────┴──────────┐
    │    Backend Server   │
    │    (Node/Python)    │
    │    PM2 / Gunicorn   │
    └──────────┬──────────┘
               │
    ┌──────────┼──────────┐
    │          │          │
  MySQL     Redis     Claude API
```

- Web 前端：Vite 构建，Nginx 托管静态文件
- 后端：PM2 (Node) 或 Gunicorn (Python) 管理进程
- 数据库：MySQL/PostgreSQL，定期备份
- Chrome 插件：打包 .crx 上传 Chrome Web Store
