# gzh-extension

Chrome Manifest V3 插件工程。

## 本地加载

1. 打开 `chrome://extensions`
2. 开启「开发者模式」
3. 点击「加载已解压的扩展程序」
4. 选择 `gzh-extension` 目录

## 使用方式

1. 在 Web 端登录后复制 JWT token
2. 打开插件弹窗，填写后端 API 地址与 JWT，并保存
3. 打开 `https://mp.weixin.qq.com` 后台页面
4. 点击右下角悬浮按钮或弹窗「开始同步」
5. 同步完成后在 Web 页面查看数据
